Algoritmo "conver"

Var
    reais, dolar, taxa: real

Inicio
    taxa <- 5.58

    Escreva("Quantos reais você tem?: ")
    Leia(reais)

    dolar <- reais / taxa

    Escreval("Com R$", reais, " você pode comprar US$", dolar, ".")

FimAlgoritmo
